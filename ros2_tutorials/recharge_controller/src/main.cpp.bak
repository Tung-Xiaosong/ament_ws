#include "rclcpp/rclcpp.hpp"
#include "recharge_controller/recharge_controller.hpp"

int main(int argc, char** argv)
{
  // 初始化ROS节点
  rclcpp::init(argc, argv);

  // 创建Node实例
  auto node = std::make_shared<recharge_controller_ns::RechargeController>();

  // 循环执行节点的回调函数
  rclcpp::spin();

  rclcpp::shutdown();

  return 0;
}
